# Environment configuration for Job Ingestion Service
# NOTE:
# - Copy this file to `.env` and adjust values for your environment.
# - This project uses Pydantic BaseSettings (v1) and will automatically read `.env`.
# - Variable names are case-sensitive in our config, keep them UPPERCASE as shown.

# Primary database connection string (SQLAlchemy/DSN format)
# Examples:
#   SQLite (local dev, default in code): sqlite:///./db.sqlite3
#   Postgres: postgresql://USER:PASS@HOST:PORT/DBNAME
DATABASE_URL=postgresql://postgres:password@localhost:5432/job_ingestion

# Redis connection URL
# Example: redis://HOST:PORT[/DB]
REDIS_URL=redis://localhost:6379

# Runtime environment name
# Common values: development | test | staging | production
ENVIRONMENT=development
